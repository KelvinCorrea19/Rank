function somar(a, b) {
    return a + b; // Retorna a soma de a e b
}

let entrada1 = 2; // Primeira entrada
let entrada2 = 3; // Segunda entrada
let resultado = somar(entrada1, entrada2); // Chama a função somar

console.log("Posicao final do heroi:", resultado);

function somar(a, b) {
    return a + b; // Retorna a soma de a e b
}

let entrada3 = 15; // Primeira entrada
let entrada4 = 3; // Segunda entrada
let resultadoo = somar(entrada3, entrada4); // Chama a função somar

console.log("Posicao final do heroi:", resultadoo);
function somar(a, b) {
    return a + b; // Retorna a soma de a e b
}

let entrada5 = 15; // Primeira entrada
let entrada6 = 3; // Segunda entrada
let resultadooo = somar(entrada5, entrada6); // Chama a função somar

console.log("Posicao final do heroi:", resultadooo);

